#!/bin/bash
# Room 03 - git init: user creates their own repo, minimal setup needed

git init 2>/dev/null
git config user.name "Escape Artist"
git config user.email "escape@rooms.dev"

echo "Room 03: Setup complete"
